apiVersion: apps/v1
kind: Deployment
metadata:
  name: myinsuranceapp-deployment-az
  labels:
    app: myinsuranceapp-az
spec:
  replicas: 5
  selector:
    matchLabels:
      app: myinsuranceapp-az
  template:
    metadata:
      labels:
        app: myinsuranceapp-az
    spec:
      imagePullSecrets:
      - name: acr-secret 
      containers:
      - name: myinsuranceapp-deployment-az
        image:  unaxmendizabal.azurecr.io/myinsuranceappunax:v8
        ports:
        - name: http
          containerPort: 5000